// stores/storage.ts
import AsyncStorage from '@react-native-async-storage/async-storage';

// createJSONStorage accepts async storages; AsyncStorage fits out of the box.
export const rnStorage = AsyncStorage;
